import './Header.scss';

const Header = (props) => {
    return (
        <div className="Header">
            <div className="Logo">Interview Reports</div>

        </div>
    )
}

export default Header;